# 2026-09-13 — home page

## Tried

User asked for a home page in the same theme: classy, minimal, unique. Not a generic AI gradient hero.

Plan: no react-router. Hash in `main.tsx` (`#` / empty = Home, `#work` = App). One `Home.tsx`, tokens already in `App.css`. Muted Home link in the sessions head so you can leave the desk.

## Worked

- Masthead (Lenny's Podcast · 301 episodes), large title, one lede, Enter CTA, numbered Ask / Write / Keep columns, rust spine on the left edge.
- Copy stays product-true (grounded Q&A, essays, briefs). No "AI-powered", no emoji, no fake social proof.
- Tablet 64px rail hides Home + Sessions label so New stays centered.

## Failed / corrected

- First CSS insert for `.home` targeted a `.home-link` block that did not exist yet; re-applied after `.eyebrow`.
- Wrapping Sessions + Home in a div would have left an empty flex item on the tablet rail and shoved New off-center. Hide `.sessions-head > div` under 1024px, restore on the mobile slide-over.

## Not done

- No react-router, no marketing illustrations.
- Did not push this slice unless asked.
